# Verification: Session Lifecycle (#22)

> Phase: Test | Started: 2026-09-25 | Status: Draft
> Requirements: [`requirements.md`](requirements.md) · Tasks: [`tasks.md`](tasks.md)

Every requirement must be traceable to at least one passing test before the Test milestone can be approved (REQ-011).

Environment: Claude Code 2.1.282, `claude --plugin-dir /home/su-sentinel/private/compass-labs` (the inline plugin replaces the cached v1.0.0; init lists all 7 `compass-labs:*` agents), repo at `4f87b94`. Every live run was headless (`-p`, stream-json with `--include-hook-events`) in throwaway git repos under the session scratchpad, with a stateful fake `gh` on PATH. Nothing was written to the real repo except this file. The real GitHub issue/PR were only read.

Row rule: where a live check against a REQ's acceptance criterion **failed**, the sub-checks that passed for that REQ are listed in the fail row's Evidence, not as separate pass rows. `check-traceability.sh` accepts any single passing row, so a separate pass row would hide the defect from the gate.

| ID | Covers REQ | Method | Result | Evidence |
|---|---|---|---|---|
| VER-001 | REQ-004, REQ-005, REQ-006, REQ-008, REQ-011, REQ-012 | unit | pass | `bash tests/run.sh` → 13 passed, 0 failed (of 13) at `4f87b94` (session_guard, session_start, check_traceability, archive_session, workflow, entry_points, phase_agents tests). |
| VER-002 | REQ-001 | e2e | pass | Live `/compass-labs:session new` (existing issue #555, sonnet, throwaway repo + bare remote + fake gh): created exactly one folder `docs/sessions/2026-09-25-widget-export/` with `log.md` (`issue: 555`, `phase: define`, `status: active`, `milestone: none`) + thin `requirements.md`, committed `2c1060f` "docs(sessions): open session #555" and pushed; `gh-setup.sh` ran (7 labels). The run hit max-turns (30) before `gh-milestone.sh none define`; about 14 of those turns went on locating SKILL.md (see VER-021). |
| VER-003 | REQ-002 | e2e | pass | Live milestone gate as `--agent compass-labs:orchestrator` (user approval pre-supplied in the prompt): frontmatter set to `milestone: design` / `phase: implement`, commit `97f4787` + push, `gh-milestone.sh 999 design implement`, then `compass-labs:implement` started with no further instruction. It committed `360c076`/`7753ae1` and returned `blocked` (sandbox refused python3). A separate resume run also started `compass-labs:design` for `phase: design` automatically. |
| VER-004 | REQ-002 | e2e | pass | `skills:` preload probe, **subagent mode** (the mode phase agents run in): random markers planted in a scratch copy of the plugin's SKILL.md files; subagents spawned via the Agent tool quoted them without calling any tool: `compass-labs:test` → `ZQX-VERIF-e4459906`, `:close` → `ZQX-DOCM-e813676a`, `:define` → `ZQX-REQS-4271c814`. The control marker in the non-preloaded `adr` skill never appeared. Preload **works** for subagents. The `--agent` main-session result is in VER-021. |
| VER-005 | REQ-003 | e2e | pass | Live resume → `compass-labs:design` returned `needs_input` → the orchestrator relayed the (pre-supplied) answers with `SendMessage` to the same agent id `ad52842a0bf0ef030` → `{"success":true,"message":"Resuming agent ad52842"}` → the same agent finished `design.md` (`status: done`). Note: with `--no-session-persistence`, `SendMessage` fails ("No transcript found for agent ID") and the orchestrator fell back to a *new* agent. |
| VER-006 | REQ-003 | manual | pending | The `AskUserQuestion` leg (the user actually answering the relayed questions) can't be driven headless; the answers were pre-supplied in VER-005. Needs one interactive run. |
| VER-007 | REQ-004 | e2e | pass | Live main-session `Write docs/sessions/2026-09-25-smoke/notes.md` → PreToolUse exit 2 "`notes.md` is not in this workflow's session file set (D1/D7)". Controls: `design.md` in the session and `src/app.txt` outside it → exit 0. |
| VER-008 | REQ-005 | e2e | pass | Live `compass-labs:define` subagent `Write design.md` → exit 2 "owned by compass-labs:design; agent_type 'compass-labs:define' may not write it (REQ-005)"; the same subagent editing `log.md` → exit 2 "written only by the orchestrator". `compass-labs:design` writing `design.md` → exit 0. |
| VER-009 | REQ-006 | e2e | pass | Live (`milestone: define`): main-session Edit of frozen `requirements.md` → exit 2 "frozen … Log a decision entry first"; `compass-labs:define` Edit of `requirements.md` → exit 2 "Subagents may never amend a frozen artifact"; Write under `docs/sessions/archive/` → exit 2. Positive path: main session after appending an uncommitted `— decision:` entry → Edit allowed (exit 0). |
| VER-010 | REQ-007 | e2e | fail | 3 of 3 live orchestrator runs (resume, relay, gate; sonnet) wrote **no `handoff` entry** for the phase-agent invocation (SKILL.md main loop step 1 requires one). The `design — decision: widget colour resolved as blue` entry and the `✅ Design complete` milestone were never added to **Key decisions**. The orchestrator also appended a phase agent's self-declared `design — milestone: design.md finalized` entry verbatim, before any user gate. Real session log: the Implement phase does have 4/4 batch handoffs, but the batch-1 "`milestone` holds the phase key" decision is only in a `note` entry and not under Key decisions, and there is no Test-phase handoff for this invocation yet. |
| VER-011 | REQ-008 | e2e | pass | Fresh process, `--agent compass-labs:orchestrator -p "resume 2026-09-25-smoke …"`: the recap matched `log.md` (frontmatter phase/milestone/next_step `SMOKE-NEXT-STEP-7Q2`, open item `OPEN-ITEM-K9`, key decision `KEYDEC-M4`, the last entries under the current phase), then it handed off to `compass-labs:design` (the correct phase). |
| VER-012 | REQ-008 | e2e | pass | Live SessionStart hook as `--agent compass-labs:orchestrator` and via `{"agent":"compass-labs:orchestrator"}` settings: stdout "Active sessions: - 2026-09-25-smoke (#999, active) — phase: design — next: SMOKE-NEXT-STEP-7Q2 …"; the `status: closed` session was excluded, and the model quoted the list without tools. Without the orchestrator agent the hook printed nothing (exit 0). |
| VER-013 | REQ-009 | e2e | fail | Live Design gate: the order was correct (commit + `git push` in one command before `gh-milestone.sh`; fake gh swapped `phase:design` → `phase:implement` only and posted the marker comment). **But milestone commit `97f4787` staged only `log.md`**: the approved artifact `design.md` stayed untracked and unpushed, so the milestone comment's link to it can't resolve. SKILL.md step d says only "`git commit` + `git push`" and never says to stage the session folder or the frozen artifact. Passing sub-checks: `gh_milestone_test.sh`/`gh_setup_test.sh` pass (VER-001 run); real issue #22 (read-only) has only `phase:test` among phase labels; the Implement comment (12:10:21Z) came after `4f87b94` (12:09:50Z, on origin); all 3 comment links return HTTP 200. |
| VER-014 | REQ-009, REQ-010 | e2e | fail | Stateful fake `gh`, partial outage: `--remove-label phase:test` fails (HTTP 502), `--add-label phase:deploy` succeeds → issue has `phase:test phase:deploy`. Re-running `gh-milestone.sh 22 test deploy` → "already has phase:deploy — label swap skipped", so the stale label is **never removed** (and a later `deploy→close` swap would leave it too). The skip condition checks only `to_label` present, not `from_label` absent. |
| VER-015 | REQ-010 | e2e | pending | Script level passes with fake gh: full outage → `gh-milestone.sh` prints 2× `SYNC_PENDING:` and exits 0, `gh-setup.sh` prints 7× `SYNC_PENDING:` and exits 0; re-running after the outage applies the swap + comment; a second run is a no-op (1 comment, 1 label). Not exercised live: the orchestrator logging the "GitHub sync pending" `note` (SKILL.md step f) and re-running the pending sync at the next gate. |
| VER-016 | REQ-011 | e2e | pass | `check-traceability.sh` on a fixture (REQ-001 pass, REQ-002 `pending`, REQ-003 no row) → exit 1, "missing a passing VER for: REQ-002, REQ-003". Also run on this session at the end of Test (see the handback). The orchestrator-side refusal (SKILL.md step a) was not exercised live. |
| VER-017 | REQ-012 | e2e | pass | Throwaway repo, `milestone: deploy`: a live `compass-labs:close` subagent (sonnet) edited only `docs/reference/api.md`: added a `GET /widget` reference and one `Origin: #777` line, no narrative (the "tried XML first" story was omitted; grep for xml/tried/we = 0). Orchestrator steps: `archive-session.sh` refused with `status: active` and on a dirty tree (exit 1); `status: archived` + `milestone: close` + milestone entry committed `fa8afc1` (the Stop guard blocked before this commit, allowed after); `git mv` → `docs/sessions/archive/2026-09-20-widget/`, committed `d3faed4`; the guard then blocked writes under `archive/` (exit 2); SessionStart listed no active sessions. Push and gh were skipped as instructed. |
| VER-018 | REQ-013 | manual | pass | Read-only `gh issue view` #24–#32: all 9 bodies name parent #22, the session path and the originating decision (Q*/D*/non-goal). All are listed in `log.md`'s header "Follow-ups: #24–#32" (#32 is missing from `requirements.md`'s follow-up table, which is frozen). The orchestrator's `gh issue create` step was not exercised live. |
| VER-019 | REQ-014 | manual | fail | `design.md` Traceability table: D1–D8 each name the REQ(s) they cover, and all 16 REQ ids appear. `tasks.md`: 13/14 tasks name a D-item; **task 3 (test harness `tests/run.sh`) names none**, which fails "every task names the design item it implements". Needs either a logged decision accepting support tasks without a DES, or a D-tag. |
| VER-020 | REQ-015 | e2e | fail | **Documented slash command does not exist**: `-p "/compass:session status"` → "I don't have a `/compass:session` command … Did you mean `/compass-labs:session`". The same wrong name appears in the README, `commands/session.md`, SKILL.md's mode table and `hooks/session-start.sh`'s "none" message. Passing sub-checks: `--agent compass-labs:<name>` resolves for all 7 agents (orchestrator, define, design, implement, test, deploy, close each replied `AGENT_OK <name>`); the repo-default `{"agent":"compass-labs:orchestrator"}` setting started the orchestrator and it listed the active session; `/compass-labs:session status` listed the session in 1 of 2 haiku runs (the other wrongly said "no active sessions"). |
| VER-021 | REQ-015 | e2e | fail | **Skill shadowed by command**: `commands/session.md` and `skills/session/` both resolve to `compass-labs:session`. The Skill tool answered "Skill /compass-labs:session is already loaded above; instructions unchanged" (the command body), so the model had to `find /` for SKILL.md. Probe: the orchestrator's `skills: [compass-labs:session]` preload injected the command marker `ZQX-CMD-55f327e5`, never the SKILL.md marker `ZQX-SESS-fb325eea`. Separately, `--agent` main-session mode injects **no** preloaded skill (test, orchestrator, close, define all answered NO_TOKENS, with tools disabled); the positive control marker in `agents/test.md`'s body was found. The orchestrator works only through its explicit read-the-file fallback. |
| VER-022 | REQ-016 | e2e | fail | **Loop guard differs from the AC**: a debug Stop hook captured the input `stop_hook_active:false` on the first Stop and `true` on the forced re-entry (CLI 2.1.282 *does* send it, contradicting task 14's finding). The hook short-circuits on `true` → the stop is allowed after **1** block, not 2; the counter stays at `1`. Passing sub-checks: an uncommitted `— decision:` entry → Stop exit 2 with reason; the model committed `c229092`; the next Stop exited 0 and the counter file was removed; `session_commit_guard_test.sh` passes (it never sends `stop_hook_active`). |
| VER-023 | REQ-016 | e2e | fail | **Counter files are never cleaned up** (known gap, not fixed): the live run left `$TMPDIR/compass-session-commit-guard/11111111-…-555555555555.count` = `1` after the session ended (the `stop_hook_active` exit path never removes it). `tests/run.sh` itself leaks 3 `test-*.count` files per run into `/tmp/compass-session-commit-guard/` (24 → 27 across one run) despite the test's `cleanup_state_files`. |

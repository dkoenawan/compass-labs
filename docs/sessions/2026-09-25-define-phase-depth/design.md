# Design: Define phase depth — problem framing, per-type standards, project anchoring

> Phase: Design | Started: 2026-09-25 | Status: Draft
> Requirements: [`requirements.md`](requirements.md) · Session history: [`log.md`](log.md)

## Approach

Framing becomes its own skill, `framing`, which names no phase. Each session type declares what it plugs into framing through a `framing` block in its workflow JSON. Two standards skills, one per artifact section, pick their rules from per-type files: `problem-statement` (new) and `requirements` (existing, restructured). The project anchor is a labelled section of the root `README.md`, wrapped in markers so it can be found without judgement calls. The framing skill defines the contract that section must meet, and #38 (`init`) will reuse that contract.

Anchor writes stay out of phase agents. Define agrees the wording with the user and records it in `requirements.md`. The orchestrator writes that text to the README in the same commit as the decision. A new Define-gate check refuses the milestone until the README contains the agreed text. This keeps the phase-agent contract unchanged, as the Constraints require.

Alternatives were put to the user and rejected, recorded in `log.md`:
- Define writing the README itself under a Close-style exception.
- A dedicated anchor agent.
- `docs/anchor.md`, or a configurable anchor location.
- Framing as a `session/reference` doc, or folded into the problem-statement skill.
- Per-type sections inside each SKILL.md.
- One combined research doc.

## Components / layers

Each design item names the `REQ-*` it covers. Paths are plugin paths unless marked as consuming-repo paths.

| ID | Component / layer | Covers | Notes |
|---|---|---|---|
| DES-001 | `skills/framing/SKILL.md`: the framing step's definition | REQ-001, REQ-002 | The phase-agnostic procedure: propose a tier, run the checks for that tier, record the outcomes in the type's framing artifact. It names no phase. Its section **"What a session type supplies"** lists the fields of the workflow `framing` block (DES-007): the artifact framing produces, the standards skills that apply, the per-type file name used inside those skills, and the tiers allowed. It checks this list against Research: a type that supplies these fields and adds its own `types/research.md` needs no change here. |
| DES-002 | Depth tiers in `framing` | REQ-003, REQ-004 | Framing proposes full, short or skip with a one-line reason, drawn from the issue and the size of the work, and asks the user through the agent's `needs_input`. The confirmed tier and reason are recorded. There is one table of every check × tier and every artifact section × tier (required or optional), with no cell left blank. Skip runs no checks, anchor ones included (Define's resolved question). Short runs every check, but each outcome can be one line. |
| DES-003 | Problem checks in `framing`: solution-first (XY) and symptom-vs-cause | REQ-005, REQ-006 | Full and short tiers only. **XY:** if the issue names a fix, record the need behind it, and either get the user to confirm the fix *is* the need or move it to "Candidate solutions". **Symptom/cause:** record the observed symptom separately from the cause, and record the cause as "unknown" when it isn't known yet. The methods applied (5 Whys and so on) are whatever the per-type standard adopts under DES-013. |
| DES-004 | Anchor checks in `framing`: locate, assess, verdict | REQ-010, REQ-020, REQ-021, REQ-022 | Full and short tiers only. **Locate:** the root `README.md` between the markers (DES-006). **Assess** against the contract checklist, with three outcomes: *complete* means use it as is and ask no anchor questions (REQ-022); *incomplete* means draft only the missing or failing elements and leave the existing wording untouched (REQ-021); *missing* (no README, or no markers) means draft vision, mission and scope, plus non-goals if the user states any (REQ-020). A README with anchor-like wording but no markers counts as *missing*, and the draft offers to reuse that wording. **Verdict:** exactly one of aligns or extends, citing the scope and non-goal lines it rests on. An *extends* verdict drafts the anchor change. In every write case, the agreed text goes into the artifact's anchor-update block and is written by the orchestrator (DES-009). |
| DES-005 | Registry/ADR overlap check in `framing` | REQ-013 | Full and short tiers only, and only where `docs/registry/index.md` or ADRs exist. The check searches the registry's "Does" column and the ADR index for overlaps with existing work and conflicts with recorded decisions. It records each one found, or "none found". The rules say explicitly that the aligns/extends verdict cites only the anchor. |
| DES-006 | `skills/framing/reference/anchor-contract.md`: the anchor contract | REQ-018, REQ-019 | **Location:** the consuming repo's root `README.md`. **Form:** a `## Project anchor` heading with `### Vision`, `### Mission`, `### Scope` and optionally `### Non-goals`, between `<!-- compass:anchor -->` and `<!-- /compass:anchor -->`. There is exactly one marker pair. **Checklist:** markers present exactly once; vision, mission and scope present and non-empty; non-goals present or not applicable; no restatement elsewhere in the README, or in any doc the README links to, that differs from the anchor without linking to it. It's stack-agnostic and assumes no `docs/` layout. #38 writes to this same contract. |
| DES-007 | `skills/session/workflows/feature.json`: add a `framing` block | REQ-001, REQ-002 | `"framing": {"run_in_phase": "define", "produces": "requirements.md", "standards": ["compass-labs:problem-statement", "compass-labs:requirements"], "type_file": "feature", "tiers": ["full", "short", "skip"]}`, with `$comment` updated to match. The phase is named here, in the workflow, never in the framing definition. The guard hook's `jq` queries don't read this key, so enforcement is unchanged. The Bugfix block itself is #24's job. |
| DES-008 | `skills/session/templates/requirements.md`: new `## Framing` section, before Problem statement | REQ-003, REQ-005, REQ-006, REQ-010, REQ-013 | It has these fields: tier and reason; the XY outcome and candidate solutions; symptom and cause; the anchor state (complete, incomplete or missing), the verdict (aligns or extends) and what it cites; an **anchor update** block holding the exact agreed text and action (create, complete or extend), or "none"; and overlaps and conflicts. At skip tier, only tier and reason are filled in. The Requirements table and its ID conventions are unchanged. Existing sessions have no Framing section and stay valid. |
| DES-009 | `skills/session/SKILL.md`: the orchestrator's anchor write and Define-gate check | REQ-011, REQ-020, REQ-021 | **Anchor write**, in main-loop step 3: when Define returns a `decision` entry for an anchor update, the orchestrator writes the anchor-update text into `README.md` between the markers. It creates the README, or appends the section, if either is missing, and never changes wording outside the elements being updated. It then commits the decision, `requirements.md` and `README.md` together. **Gate check** "a3 — Define milestone only": if `requirements.md`'s Framing section shows full or short tier and an anchor-update action other than "none", the README's marked section must contain that text and pass the DES-006 checklist. Otherwise the milestone is refused and the gap shown. A session with no Framing section gets no check. |
| DES-010 | `agents/define.md` | REQ-001, REQ-007, REQ-008 | Preload `compass-labs:framing`, `compass-labs:problem-statement` and `compass-labs:requirements`, and read the `framing` block from the session type's workflow. Read `types/{type_file}.md` in each standards skill. Keep the existing fallback: read a SKILL.md from disk if its preload isn't visible. The description drops "turns the problem statement into" and becomes "frames the problem and writes requirements". Contract text is unchanged. |
| DES-011 | `skills/problem-statement/` (new): `SKILL.md`, `types/feature.md`, `types/bugfix.md`, `reference/methods.md` | REQ-007 | SKILL.md covers what every problem statement has: no solution talk, who is affected, why it matters. Each type file lists that type's required content and its applicable methods, citing each method's adopt or adapt verdict in `reference/methods.md`. Expected shapes: Feature is the need, who has it, and the impact. Bugfix is the observed symptom, reproduction, expected vs actual, impact, and cause (known or unknown). The final method lists follow the DES-013 verdicts. |
| DES-012 | `skills/requirements/` (restructured): SKILL.md plus `types/feature.md`, `types/bugfix.md`, `reference/methods.md` | REQ-008, REQ-009 | The shared rules stay in SKILL.md: `REQ-*` IDs never reused, struck-through rows, one Given/When/Then per row in the same table, and the 29148 checklist. So `check-traceability.sh` parses both types the same way. The type files hold content and methods. Feature is the current EARS standard. Bugfix is corrected-behaviour `REQ-*` rows plus a regression criterion (expected shape, final per DES-013). The "deeper research tracked in #32" line is removed. |
| DES-013 | Methods research records: `skills/problem-statement/reference/methods.md` and `skills/requirements/reference/methods.md` | REQ-014, REQ-015 | **Carried out in Implement**, as its own tasks, before DES-011 and DES-012's type files are finalised. Each file is a table of candidate method × Feature and Bugfix verdicts (adopt, adapt or reject), with a 1–2 sentence rationale and, for adopt and adapt, the condition under which the method applies. Candidates are from Scope item 7: problem statement is XY, SCQ/Minto, symptom-vs-cause, 5 Whys, JTBD and PR-FAQ; requirements is EARS, Given/When/Then and 29148. A separate "Additions" table allows at most 3 per file, each naming the gap it fills. A "Deferred" list holds any other proposals, each with a follow-up issue number, opened in the same turn. The research is done when every candidate has both verdicts. |
| DES-014 | compass-labs `README.md`: anchor and full refresh | REQ-016 | This is the first anchor written to DES-006. Implement drafts vision, mission, scope and non-goals, gets the user's approval through the orchestrator, and puts them between the markers. The title and body use the plugin's name from `plugin.json`, never "Systematic Dev Kit", and drop the full-stack framing. Every `skills/*/SKILL.md` and `agents/*.md` gets an entry, including new `framing` and `problem-statement`, and nothing is listed that doesn't exist (for example the tree currently omits `adr`, `doc-maintainer`, `post-hook-validator` and `task-executor`). The command prefix is documented as it is today (#34). |
| DES-015 | compass-labs `docs/explanation/solution-design.md`: full refresh | REQ-017, REQ-019 | "What It Does" and "Who It's For" link to the README anchor instead of restating it. The name is fixed, and the domain map covers every `skills/*` directory, adding `framing`, `problem-statement`, `adr`, `post-hook-validator`, `task-executor` and any others found. |
| DES-016 | `docs/registry/decisions/003-framing-and-project-anchor.md` plus an index row | REQ-001, REQ-018 | Written in Implement. It records D1–D5 below as one decision: shared framing skill, per-type blocks, README anchor contract, orchestrator-applied anchor writes. |

## Decisions

The ADR is [ADR-003](../../registry/decisions/003-framing-and-project-anchor.md), written in Implement (DES-016). D1–D5 were agreed with the user on 2026-09-25.

| # | Decision | Notes |
|---|---|---|
| D1 | The orchestrator applies every anchor write. Define only drafts it, and a Define-gate check (a3) enforces REQ-011. | This keeps the phase-agent contract unchanged. The guard hook doesn't cover paths outside `docs/sessions/`, so the contract text is the only guard there. Keeping anchor writes in the orchestrator avoids a second unenforced exception like Close's. |
| D2 | The anchor is a `## Project anchor` section in the root README, between `compass:anchor` markers. | The README is where a newcomer looks first, and every repo has one or can have one. The markers make detection mechanical (REQ-018, REQ-022). A configurable location could be added later without breaking this. |
| D3 | Framing is its own skill. Session types plug in through a `framing` block in `workflows/<type>.json`. | The phase is named only in the workflow (REQ-001). Research (#25) adds a block and type files, nothing else (REQ-002). |
| D4 | There is one standards skill per artifact section (`problem-statement`, `requirements`), each with `types/{feature,bugfix}.md` read on demand. | A session loads only its own type. `requirements` keeps its name, so existing preloads don't break. |
| D5 | Each section's methods verdicts live beside its standard in `reference/methods.md`. The research is done in Implement. | The ≤3-additions limit is counted per section, as REQ-015 requires. The standards cite verdicts next to them (REQ-007, REQ-008). |
| D6 | Pre-change sessions are grandfathered. The Framing section and the gate check apply only when the Framing section is present. | This satisfies the "existing sessions stay valid" constraint. Traceability parsing is unchanged. |

## Risks

| Risk | Mitigation |
|---|---|
| Hand edits, or other tooling, break or duplicate the anchor markers in a README. | DES-006's checklist requires exactly one marker pair. Framing reports a broken pair as *incomplete* and drafts a repair, which goes through D1 like any other anchor write. |
| Preloading three skills into Define adds token load, and `skills:` preload has an unconfirmed gap. | The type files are read on demand, and SKILL.md bodies stay short. Keep define.md's read-from-disk fallback for every preloaded skill (DES-010). |
| The research grows beyond its limit, or delays the standards. | Scope item 7's stopping point applies. Additions are capped at 3 per section, anything else becomes a follow-up issue, and the research tasks come before the type files so there's no rework. |
| Restructuring `requirements` changes how existing sessions or `check-traceability.sh` read requirements. | The ID and table rules stay in SKILL.md unchanged (DES-012). Implement adds a `tests/` case that runs `check-traceability.sh` on a requirements.md written under the new template and on an existing one (REQ-009). |
| Adding a `framing` key to `feature.json` breaks the hooks or tests that read it. | The hook queries select only `phases[]` and `session_level`. Run `tests/run.sh` after the change. |
| The orchestrator's anchor write overwrites the user's wording. | DES-009 writes only the elements named in the agreed anchor-update block. REQ-021's criterion (existing vision and mission unchanged) becomes a VER check. |

## Open questions

All questions resolved. The wording of compass-labs' own vision and mission is approved by the user during Implement (DES-014), not settled here.
